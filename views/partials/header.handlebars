<div class="container">
  <div id="login">
      <div class="row">
          <div class="col s4">
              <img id="logo" class="reponsive-img" src="/img/logo.png" alt="TrekMate">
          </div>
          <div class="col s2"></div>
          <div class="col s6">
              <div class="nav-links">
                <ul class="nav masthead-nav">
                  {{#if user }}
                  <li class="active">
                      <button type="button" class="btn btn-primary" onclick="location.href = '/dashboard';" >
                        Your Trips
                      </button>
                  </li>
                  <li class="active">
                      <button type="button" class="btn btn-primary" onclick="location.href = '/logout';" >
                        Log Out
                      </button>
                  </li>
                  {{else}}
                    <li class="active">
                        <button type="button" class="btn btn-primary" data-toggle="modal" data-target="#signInModal">
                          Sign in
                        </button>
                    </li>
                    <li>
                        <button type="button" class="btn btn-primary" data-toggle="modal" data-target="#signUpModal">
                          Sign Up
                        </button>
                    </li>
                    <li>
                      <button type="button" class="btn btn-primary" data-toggle="modal" data-target="#aboutModal">
                        About
                      </button>
                    </li>
                    {{/if}}
                  </ul>
              </div>
          </div>
      </div>
    </div>
  </div>
